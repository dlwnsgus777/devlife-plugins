# spec: [기능명] ([티켓 번호])

## 0. 코드 구조 정비 — 기존 코드 수정 시만 작성

> 신규 파일만 추가하는 경우 생략. 정비 커밋은 기능 커밋과 **반드시 분리**한다.

### 0-1. Tidy First (동작 변경 없는 정비)

| 파일 / 클래스 | 문제점 | 정비 기법 |
|-------------|------|---------|
| `[ClassName]` | [예: 중첩 if로 케이스 추가 어려움] | [예: Guard Clause / Extract Method] |

### 0-2. 추상화 제안 (변화 지점)

> 케이스가 2개 이상(또는 이번 요구사항이 두 번째)이고, 도메인 문맥상 계속 늘어나는 축에만 제안한다.
> 케이스가 1개면 추상화하지 않는다. 해당 없으면 **`해당 없음`이라고만 적는다** — 억지로 채우지 않는다.

| 변화 지점 | 도메인 근거 | 제안 추상화 | 적용 여부 |
|---------|-----------|-----------|---------|
| [예: `SettlementService`의 채널별 수수료 분기] | [예: 채널이 3개이며 신규 채널 온보딩이 분기별로 발생] | [예: `FeePolicy` 인터페이스 + 채널별 구현체] | [승인 대기 / 적용 / 보류] |

**커밋 순서**: `refactor: [정비 내용]` → `feat([티켓]): [기능 내용]`

---

## 1. 기능 개요

[2-3문장으로 기능의 목적과 배경 설명]

### 화면 구성 / 기능 구성

| 섹션 / 기능 | 표시 데이터 / 설명 |
|------------|------------------|
| **[섹션명]** | [데이터 항목] |
| **[섹션명]** | [데이터 항목] |

---

## 2. 도메인 문맥 및 불변성

### 도메인 문맥

[이 기능이 속한 도메인의 핵심 개념과 전제를 2-4문장으로 서술한다.
예: "계약은 초안 → 승인 대기 → 승인 완료 → 해지의 단방향 상태 머신을 따르며, 각 상태 전이는 특정 권한을 가진 사용자만 수행할 수 있다."]

### 비즈니스 불변성

> 구현 전 과정에서 절대 깨져서는 안 되는 규칙을 **완성된 문장**으로 작성한다.
> 불변성이 깨졌을 때 어떤 문제가 생기는지 함께 서술한다.
> ID는 섹션 8과 tdd-team이 참조하므로 한 번 부여하면 바꾸지 않는다.
> 출처는 `[문서]` / `[사용자확인]` / `[코드추론]` 중 하나 — `[코드추론]`은 근거가 가드 절뿐이라는 뜻이므로 검토 우선순위가 가장 높다.
>
> **격리 불변성**: 기존 구현과 나란히 도는 신규 구현(별도 테이블·별도 플로우)이라면
> "신규가 기존 것을 건드리지 않는다"와 **그 역방향**을 각각 불변성으로 적는다.
> tdd-team이 이 ID를 물려받아 양쪽 경계를 검증하므로, 여기 적으면 검증이 따라온다.

| ID | 불변성 | 위반 시 영향 | 출처 |
|----|-------|------------|------|
| INV-001 | [예: 승인 완료된 계약의 금액은 어떠한 경우에도 변경될 수 없다] | [예: 정산 불일치와 감사 실패] | `[사용자확인]` |
| INV-002 | [예: 계약 해지 요청은 원계약이 존재하는 상태에서만 가능하다] | [예: 고아 해지 레코드 생성] | `[코드추론]` |
| INV-003 | [격리 예: 신규 처리는 기존 `legacy_table`을 갱신하지 않는다] | [예: 데이터 오염·정산 불일치] | `[문서]` |
| INV-004 | [역방향 예: 기존 처리는 신규 `new_table`을 갱신 대상으로 조회하지 않는다] | [예: 역방향 오염] | `[문서]` |

---

## 3. 기존 코드의 함정 (답습 금지)

> 참조할 기존 구현에 결함이 있으면 여기 적는다. **적지 않으면 AI는 기존 코드를
> "이 프로젝트의 정답"으로 읽고 그대로 복제한다.**
> 각 항목은 문제 진단으로 끝내지 말고 **`→ 신규는`까지** 쓴다 — 지시가 없으면 대안을 임의로 발명한다.
> 참조할 기존 구현이 없거나 결함을 찾지 못했으면 **`해당 없음`이라고만 적는다.**

### 3-1. [함정 제목 — 증상으로 명명]

**현재 코드** — `[경로:라인]`: [무엇을 하는가]

**왜 문제인가**: [재현 조건 / 경계값 / 인덱스·성능 영향]

| 조건 | 현재 결과 | 기대값 |
|------|---------|-------|
| [입력] | [잘못된 값] ❌ | [올바른 값] |

**→ 신규는**: [대신 무엇을 하는가]

**기존 코드 수정 여부**: [이번 티켓에서 함께 고침 / 후속 티켓으로 분리 — 이유]

---

## 4. API 설계

### API 1 - [API 이름]

```
[HTTP METHOD] [/path/to/endpoint]
Authorization: Bearer {token}
```

- [이 API가 하는 일 한 줄 설명]

**Request**
```json
{
  "field1": "value",
  "field2": 0
}
```

**Response 200 OK**
```json
{
  "field1": "value",
  "field2": 0
}
```

**엣지 케이스 (예: 데이터 없을 경우)**
```json
{
  "field1": null
}
```

---

### API 2 - [API 이름] (필요 시)

```
[HTTP METHOD] [/path/to/endpoint]
```

**Response 200 OK**
```json
{}
```

---

## 5. 비즈니스 로직

### 5-1. [주요 로직 1]

- [처리 흐름 설명]
- [사용할 서비스/메서드]
- [예외 케이스]

### 5-2. [주요 로직 2]

- [처리 흐름 설명]
- [사용할 서비스/메서드]

### 5-3. [상태값 / 매핑 규칙] (필요 시)

| 내부 값 (enum/DB) | 표시 / 변환 값 |
|-------------------|--------------|
| [VALUE_A]         | [표시 문자열] |
| [VALUE_B]         | [표시 문자열] |

---

## 6. 구현 대상 파일

### [모듈명] 모듈

| 파일 | 역할 |
|------|------|
| `[ClassName]` | [역할 설명] |
| `[ClassName]` | [역할 설명] |

### [다른 모듈명] 모듈 변경사항

- [변경 내용] 또는 "변경 없음 — 기존 서비스 재활용"
  - `[ServiceClass.method()]` ← [용도]

---

## 7. 주요 고려사항 & 질문

1. **[고려사항 제목]**: [내용 및 확인 필요 여부]
   - 대안: [대안 설명]

2. **[고려사항 제목]**: [내용]

3. **[고려사항 제목]**: [내용]

---

## 8. 구현 순서 (TDD)

> 각 테스트의 DisplayName은 구현 클래스명이 아닌 **도메인 규칙 문장**으로 작성한다.
> 예: ~~"testCancelWhenPaid"~~ → **"결제 완료된 주문은 취소할 수 없다"**
> 테스트 항목에는 검증하는 불변성 ID를 함께 적는다. 섹션 2의 모든 ID가 여기 한 번 이상 나와야 한다.

### 1단계: 코드 정비 (Tidy First) — 기존 코드 수정 시만

1. [ ] 정비: [기법] — `[대상]`

### 2단계: 기능 구현

2. [ ] [구현 대상] — [설명]
3. [ ] `[NEW]` `INV-001` [테스트 클래스명] — `"[도메인 규칙 문장]"`
4. [ ] `[REGRESSION]` `INV-002` [기존 테스트 클래스명] — `"[도메인 규칙 문장]"`
5. [ ] `[NEW]` [통합 테스트] — `"[시나리오 문장]"`

---

## 9. 인수 조건 (Acceptance Criteria)

- [ ] [조건 1]
- [ ] [조건 2]
- [ ] [조건 3]
