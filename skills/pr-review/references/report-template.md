# PR 리뷰 리포트 템플릿

```markdown
# PR 리뷰 — #{PR_ID} {title}

> 작성일: {YYYY-MM-DD} | 작성자: {author} | 브랜치: `{source}` → `{destination}`
> 도메인 문서: {참조한 docs/domain/*.md 파일명 | 없으면 생략}

---

## 변경 파일 목록

| 상태 | 파일 |
|------|------|
| M | path/to/changed/File.java |
| A | path/to/new/File.java |
| D | path/to/deleted/File.java |

---

## 코드리뷰

<!-- 파일별로 그룹화. 발견이 없는 파일은 생략. -->
<!-- 의도가 명확하지 않은 경우: 문제로 단정하지 말고 "의도 확인 필요" 섹션에 질문으로 작성 -->
<!-- 스타일/취향 차이는 팀 컨벤션 합의 없이 P3 이상 부여 금지 -->

### `경로/파일명.java`

#### [P1] 제목: 한 줄로 핵심 문제 요약

```java
// 문제가 되는 실제 코드 스니펫 (diff에서 발췌, 5~15줄 이내)
```

**이유:** 왜 문제인지 맥락과 근거 설명

**방향:** 어떤 키워드/방향으로 개선할지 제안 (완성 코드 금지)

---

#### [P3] 제목: 또 다른 발견

```java
// 실제 코드
```

**이유:** ...

**방향:** ...

---

### `다른파일.java`

#### [P2] ...

---

## 의도 확인 필요

<!-- 문제인지 아닌지 단정하기 어렵고, 먼저 이유를 들어야 판단 가능한 코드 -->

#### `경로/파일명.java:라인번호` — 궁금한 점

```java
// 해당 코드 스니펫
```

이렇게 구현하신 특별한 이유가 있을까요?
〇〇 상황을 고려한 것인지, 아니면 〇〇 방향도 검토해보셨는지 궁금합니다.

---

## 잘된 점

<!-- 리뷰할 것이 없으면 이 섹션을 충분히 채운다. 추상적 칭찬 금지 — 코드 인용 필수. -->

#### `경로/파일명.java:라인번호` — 구체적 패턴명

```java
// 칭찬할 실제 코드 스니펫
```

**이유:** 어떤 점에서 좋은지, 왜 이 선택이 올바른지 구체적으로 설명

---

## 점수 요약

| 차원 | 점수 | 등급 |
|------|------|------|
| 코드 컨벤션 (25%) | XX | X |
| 테스트 품질 (30%) | XX | X |
| 도메인 로직 (30%) | XX | X |
| 설계 품질 (15%) | XX | X |
| **종합** | **XX** | **X** |

Grades: A(90+) B(80+) C(70+) D(60+) E(50+) F(<50)
Overall = (컨벤션×0.25) + (테스트×0.30) + (도메인×0.30) + (설계×0.15)

---

## 우선 개선 항목

| 우선순위 | 파일:라인 | 요약 |
|----------|-----------|------|
| P1 | `파일명:45` | 한 줄 요약 |
| P2 | `파일명:120` | 한 줄 요약 |

---

## 종합 의견

머지 승인 여부 및 핵심 메시지 (3~5줄)
```
